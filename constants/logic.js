const clusterSize = 4096;
const quality = 50;
const typeOfFileMaxLength = 5;
const undefinedFileType = 'undefined';
const typeOfResults = {
  avg: 'avg',
  min: 'min',
  max: 'max',
  numF: 'numF',
};

module.exports = {
  clusterSize,
  quality,
  typeOfFileMaxLength,
  undefinedFileType,
  typeOfResults,
};